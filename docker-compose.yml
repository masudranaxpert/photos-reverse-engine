services:
  instant-engine:
    build: .
    container_name: instant-engine
    restart: unless-stopped
    ports:
      - "8890:8000"
    env_file:
      - path: .env
        required: false
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
